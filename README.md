# Colorful Responsive Site

Preview the site by opening `index.html` in a browser. The design includes a left-side sidebar (collapses on small screens), Home, Inventory, and Top Up pages.

Files:
- `index.html` — main single-file pages
- `styles.css` — theme and responsive layout
- `script.js` — navigation and demo data
